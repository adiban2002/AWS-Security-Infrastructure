apiVersion: v1
kind: Service
metadata:
  name: devsecops-service
  labels:
    app: devsecops-app
spec:
  type: NodePort 
  selector:
    app: devsecops-app
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 8003 